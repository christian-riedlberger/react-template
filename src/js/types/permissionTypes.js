// @flow

/**
 *  Permission prop types
 */
export type PermissionLevels = {
    create: boolean,
    write: boolean,
    clone: boolean,
    destroy: boolean,
    comment: boolean,
    lockdown: boolean,
    all: boolean
};
